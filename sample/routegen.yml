apps:
  app:
    routes:
      user: /app/users/:id/:subview(profile|pictures)?
      account: /app/account
    routingType: ReactRouterV5
    destinationDir: sample/output/app/routes

  auth:
    routes:
      login: /login
      signup: /signup
    routingType: ReactRouterV5
    reactRouterV5LinkOptions:
      importCustomLink:
        componentDefaultImport: true
        propsNamedImport: LinkProps
        hrefProp: to
        from: common/components/Link
      useParams: false
      useRedirect: false
    defaultLinkOptions:
      importCustomLink:
        componentNamedImport: CustomAnchor
        propsNamedImport: AnchorProps
        hrefProp: href
        from: common/ui/Anchor
    destinationDir: sample/output/auth/routes

  seo:
    routes:
      home: /
      about: /about/:target(us|you)/:topic/:optional?/:optionalEnum(enumOne|enumTwo)?
    routingType: NextJS
    destinationDir: sample/output/seo/routes

  toc:
    routes:
      toc: /terms-and-conditions
    routingType: NextJS
    nextJSLinkOptions:
      importCustomLink:
        componentDefaultImport: true
        propsNamedImport: LinkProps
        hrefProp: href
        from: src/common/components/Link
    defaultLinkOptions:
      importCustomLink:
        componentDefaultImport: true
        propsNamedImport: AnchorProps
        hrefProp: href
        from: src/common/ui/Anchor
    destinationDir: sample/output/toc/routes

  legacy:
    routes:
      legacy: /legacy/app

  server:
    generateLink: false
    destinationDir: sample/output/server/routes
