apps:
  app:
    routes:
      user: /app/users/:id/:subview(pictures)?
      account: /app/account
    routingType: ReactRouterV5
    destinationDir: output/default/app/routes

  auth:
    routes:
      login: /login
      signup: /signup
    routingType: ReactRouterV5
    generateLinkComponent: true
    generateUseParams: true
    generateUseRedirect: true
    reactRouterV5LinkOptions:
      importCustomLink:
        componentDefaultImport: true
        propsNamedImport: LinkProps
        hrefProp: to
        from: common/components/Link
      generateUseParams: false
      generateUseRedirect: false
    defaultLinkOptions:
      importCustomLink:
        componentNamedImport: CustomAnchor
        propsNamedImport: AnchorProps
        hrefProp: href
        from: common/ui/Anchor
    destinationDir: output/default/auth/routes

  seo:
    routes:
      home: /
      about: /about/:target(us|you)/:topic/:optional?/:optionalEnum(enumOne|enumTwo)?
    routingType: NextJS
    destinationDir: output/default/seo/routes

  toc:
    routes:
      toc: /terms-and-conditions
    routingType: NextJS
    nextJSLinkOptions:
      importCustomLink:
        componentDefaultImport: true
        propsNamedImport: LinkProps
        hrefProp: href
        from: src/common/components/Link
    defaultLinkOptions:
      importCustomLink:
        componentDefaultImport: true
        propsNamedImport: AnchorProps
        hrefProp: href
        from: src/common/ui/Anchor
    destinationDir: output/default/toc/routes

  legacy:
    routes:
      legacy: /legacy/app

  server:
    generateLink: false
    destinationDir: output/default/server/routes
