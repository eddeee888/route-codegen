apps:
  app:
    origin: https://app.${PRIMARY_DOMAIN}
    routes:
      user: /users/:id/:subview(pictures)?
    routingType: ReactRouterV5
    destinationDir: output/with-origins/app/routes

  seo:
    origin: https://${PRIMARY_DOMAIN}
    routes:
      home: /
      terms: /terms
    routingType: NextJS
    destinationDir: output/with-origins/seo/routes

  legacy:
    origin: https://legacy.domain.com
    routes:
      legacy: /legacy/app

  api:
    origin: https://api.${PRIMARY_DOMAIN}
    routes:
      graphql: /graphql
      activateAccount: /activate-account/:code
    generateLink: false
    destinationDir: output/with-origins/server/routes
